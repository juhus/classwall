$widgets = $('<%="#widget-#{@comment.widget_id}"%>');
$widgets.find('.widget-addcomment').text('Comment');
$container.masonry('remove',$widgets.find('.new-comment'));
$widgets.find('.widget-footer:first').after('<%= escape_javascript(render :partial => "comment", :locals => { :comment => @comment })%>');
$container.masonry('reload');