$items = $('.item.new');
$container.masonry( 'remove', $items );
$itemContent=$('<%= escape_javascript(render :partial => "experience", :locals => { :experience => @experience })%>');
$container.prepend($itemContent).masonry('reload');